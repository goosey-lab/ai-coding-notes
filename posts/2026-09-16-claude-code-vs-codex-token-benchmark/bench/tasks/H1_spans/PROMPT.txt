Users report that spans.py returns wrong results and is far too slow for calendars with hundreds of thousands of spans. Fix spans.py so that every function behaves exactly as its docstring describes, and so that normalize, total_length, subtract, free_slots and find_slot run in O(n log n) time. Keep the public API unchanged. test_spans.py contains a few examples; do not modify it.
